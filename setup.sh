#!/bin/bash

# first clone the repository and cd MemoMind

# Install npm dependencies
npm install

# Run the development server
npm run dev

# make sure to make script executable by doing chmod +x setup.sh in terminal before running with ./setup.sh
